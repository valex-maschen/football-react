const MATCHES = '/matches';
const MATCH = `${MATCHES}/:id`;
const TEAMS = '/teams';
const TEAM = `${TEAMS}/:id`;
const LEAGUES = '/leagues';

export const Routes = {
    MATCHES,
    MATCH,
    TEAMS,
    TEAM,
    LEAGUES
};