const MATCHES = '/matches';
const MATCH = `${MATCHES}/:id`;
const TEAMS = '/teams';
const TEAM = `${TEAMS}/:id`;
const LEAGUES = '/leagues';
const COMPS = '/competitions';
const COMP = `${COMPS}/:id`;

export const Routes = {
    MATCHES,
    MATCH,
    TEAMS,
    TEAM,
    LEAGUES,
    COMPS,
    COMP,
};